# Written by Richard Christopher, Copyright 2026 Richard Christopher
# Aliases never cross a document boundary, so every merge fixture keeps its
# source and its consumer in the SAME document.
--- !node
base: &base
  host: localhost
  port: 80
web:
  <<: *base
  port: 8080
