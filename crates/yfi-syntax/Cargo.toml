# Written by Richard Christopher, Copyright 2026 NeoTec, LLC
# SPDX-License-Identifier: GPL-3.0-or-later

[package]
name = "yfi-syntax"
description = "Event-level YAML parsing into an arena AST with spans and accumulating diagnostics."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
# Fixtures live at the workspace root, outside this crate, so the packaged
# crate must not carry tests that cannot find them.
exclude = ["tests"]

[dependencies]
saphyr-parser.workspace = true
tracing.workspace = true

[lints]
workspace = true
